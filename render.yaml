services:
  - type: web
    name: garage-management-system
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 test-backend.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
